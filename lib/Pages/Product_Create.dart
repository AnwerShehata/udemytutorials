import 'package:flutter/material.dart';

class Product_Create extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return new Scaffold(



      body: Center(child: new Text("Product_Create"),),
    );

  }
}